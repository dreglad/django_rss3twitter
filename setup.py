from setuptools import setup, find_packages

setup(
    name = "rss3twitter",
    version = "0.1",
    url = '',
    description = "rss3twitter",
    packages = find_packages('.'),
    package_dir = {'': '.'},
    install_requires = ['setuptools'],
)
